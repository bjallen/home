<%= form_for(@thermometer) do |f| %>
  <% if @thermometer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thermometer.errors.count, "error") %> prohibited this thermometer from being saved:</h2>

      <ul>
      <% @thermometer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spark_core_id %><br>
    <%= f.select :spark_core_id, @spark_cores.map { |core| [core.name, core.id] } %>
  </div>
  <div class="field">
    <%= f.label :room %><br>
    <%= f.text_field :room %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
